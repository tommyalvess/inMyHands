import { StyleSheet} from 'react-native';

export const estilo = StyleSheet.create({

    container: {
        width: 49,
        height: 49,
        borderRadius: 45,
        alignItems: 'center',
        justifyContent: 'center',
        marginRight: 22
    },

})